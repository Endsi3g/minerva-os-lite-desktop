export const metadata = {
  title: 'Séquences & Cadences',
  description: "Planifie et automatise tes relances par lead : séquences automatiques, modèles, file d'attente et calendrier de relances.",
};

import { SequencesRoot } from './_components/sequences-root';

export default function SequencesPage() {
  return <SequencesRoot />;
}
