export const metadata = {
  title: 'Séquences, Composer & Cadences',
  description: "Séquences automatiques par lead, modèles, rédaction manuelle, file d'envoi et calendrier de relances — tout l'outreach par email au même endroit.",
};

import { SequencesRoot } from './_components/sequences-root';

export default function SequencesPage() {
  return <SequencesRoot />;
}
