import { RecoveryRoot } from './_components/recovery-root';

export const metadata = { title: 'Récupération de données — Minerva' };

export default function RecoveryPage() {
  return <RecoveryRoot />;
}
