export const metadata = {
  title: 'Configuration',
  description: 'Finalisez la configuration de votre compte Minerva.',
};

import { SetupRoot } from './_components/setup-root';

export default function SetupPage() {
  return <SetupRoot />;
}
